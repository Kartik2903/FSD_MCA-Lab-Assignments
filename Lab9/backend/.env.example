# Server
PORT=3000
CORS_ORIGIN=http://localhost:5173

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=securespace

# Nodemailer SMTP (Gmail example with App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=kartikdewnani56@gmail.com
SMTP_PASS="jdfg hhaj poes xdbw"
MAIL_FROM="SecureSpace Kartik"

# Public URL of backend (for image links in emails)
PUBLIC_BASE_URL=http://localhost:3000